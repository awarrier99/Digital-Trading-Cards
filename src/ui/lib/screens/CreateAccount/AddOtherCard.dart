import 'package:flutter/material.dart';
import 'package:ui/components/SearchBarComponent.dart';

class AddOtherCard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SearchBarComponent(),
    );
  }
}
